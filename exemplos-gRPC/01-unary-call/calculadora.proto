syntax = "proto3";

package calculadora;

service Calculadora {
  rpc Somar(RequisicaoSoma) returns (RespostaSoma) {}
}

message RequisicaoSoma {
  int32 num1 = 1;
  int32 num2 = 2;
}

message RespostaSoma {
  int32 resultado = 1;
}